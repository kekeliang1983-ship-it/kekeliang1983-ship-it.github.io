# CozyCreatures · 治愈小生物 —— 部署到 GitHub Pages 完整指南

> 本文档记录本项目从「本机开发」到「公网可访问」的**全部部署流程、踩坑与日常使用手册**。
> 目的：让你在任意时间回看都能完整复现、独立维护，不依赖当时的对话记录。
> 最后更新：2026-09-06

---

## 〇、成果速览（先看这个）

| 项目 | 内容 |
|---|---|
| **公网网址** | https://kekeliang1983-ship-it.github.io/ |
| **GitHub 仓库** | https://github.com/kekeliang1983-ship-it/kekeliang1983-ship-it.github.io |
| **仓库形式** | 根仓库（`<用户名>.github.io`），挂在域名根，零代码改动上线 |
| **部署方式** | GitHub Actions 自动部署（push 到 `main` 即自动构建+发布） |
| **本地开发地址** | http://localhost:5173 （`npm run dev`），含调参后台 |
| **提交身份** | `kekeliang1983-ship-it` / `kekeliang1983-ship-it@users.noreply.github.com` |
| **默认分支** | `main` |

**一句话**：任何人用上面的网址即可打开玩游戏；你改完内容/代码，`git push` 一下约 1 分钟自动上线，玩家刷新即得。

---

## 一、核心概念（为什么这么部署）

### 1.1 GitHub ≠ GitHub Pages
- **GitHub**：代码仓库（网盘），存代码，**本身不能让你用链接打开游戏**。
- **GitHub Pages**：GitHub 附带的**免费静态网站托管**。把构建出的网页文件丢上去，就得到 `https://用户名.github.io/仓库名` 的网址。本项目用的是「根仓库」形式，网址落在 `https://用户名.github.io/` 根目录。

### 1.2 根仓库 vs 普通仓库（本项目选了根仓库）
- **根仓库**：仓库名恰好是 `<用户名>.github.io`，网站挂在 `https://用户名.github.io/` 根目录。代码里的绝对路径 `base:'/'`、`/content`、`/sw.js` **全部成立，零代码改动即可上线**。代价：一个 GitHub 账号只能有这一个根站。
- **普通命名仓库**（如 `cozycreatures`）：网址带 `/仓库名/` 子路径，需改 `vite.config` 的 `base`、内容 `BASE`、SW 注册等绝对路径才能正常加载。适合后续建多站或绑自定义域名。

> 本项目是纯前端 Vue3 + Vite 静态站（无后端），GitHub Pages 量身定做；且使用 **hash 路由**（`#/admin`、`#/race`），在子路径下刷新不会 404，部署最省心。

### 1.3 本项目的三个部署关键点（决定了这样配）
1. **内容是外置的**：所有玩法数值在 `public/content/*.json`，运行时由 `useContentStore` 拉取（`BASE = '/content'`）。
2. **Service Worker 缓存优先**：`public/sw.js` 是「缓存优先」策略，会缓存静态资源——**必须显式排除 `/content/*`**，否则线上改数值玩家刷新看不到。
3. **后台 admin 是 DEV-only**：生产构建不含后台（只有本机 `npm run dev` 才有），所以线上玩家看到的是纯游戏，这正是正确的。

### 1.4 线上「改内容怎么生效」（重要）
- 你改 `public/content/*.json` → `git commit` → `git push` → Actions 自动重部署（约 1 分钟）→ **玩家刷新页面即得新版**。
- SW 已配置**不缓存 `/content/*`**，所以永远不会被旧缓存拖住（详见第三节守卫代码）。
- 玩家进度（货币/连胜/称号）存在另一套持久化里，**刷新只换配置、不丢存档**。

---

## 二、前置条件

| 条件 | 状态 | 说明 |
|---|---|---|
| GitHub 账号 | ✅ 已有 | 用户名 `kekeliang1983-ship-it` |
| GitHub CLI (`gh`) | ✅ 已装 | 2.100.0，由 `winget install --id GitHub.cli` 安装 |
| `gh` 登录态 | ✅ 已登录 | 浏览器授权登录（详见步骤一） |
| Node / npm | ✅ 已有 | 项目用 Vite5，Actions 里用 Node 20 |
| 本地 git | ✅ 已配置 | 已 `git init`、分支 `main`、身份已设 |

> ⚠️ **关键提醒**：本机对话环境用的是 PortableGit bash，其 `gh` 路径与凭据读取出过问题。但**你在自己电脑的 CMD/终端**里登录的 `gh` 凭据存在本机，`gh repo create`/`git push` 由你本机终端执行最稳妥。下面的命令你都可以直接复制到自己的 CMD 里跑。

---

## 三、完整部署流程（逐步）

### 步骤一：安装 GitHub CLI 并登录

**1. 安装（在你的 CMD / PowerShell 里）**
```powershell
winget install --id GitHub.cli
```
装完验证（可能需要重开终端让 PATH 生效）：
```powershell
gh --version
```

**2. 登录授权**
```powershell
gh auth login
```
按屏幕提示操作，每一步的选择如下：

| 提示 | 选择 |
|---|---|
| `What account do you want to log into?` | `GitHub.com` |
| `How would you like to authenticate?` | **`Login with a web browser`** ✅（推荐） |
| 协议选择（若问 HTTPS or SSH） | **HTTPS** ✅（`gh` 会自动管理凭据，无需再输密码） |
| Credential helper（若问） | 选它推荐的那项（Git Credential Manager） |

**3. 完成浏览器侧授权**
- 选 `Login with a web browser` 后，终端会显示一串**一次性验证码**（形如 `2660-0613`），并自动打开浏览器。
- 在浏览器里粘贴/确认该码 → 进入 GitHub 授权页 → 点绿色 **`Authorize github`** 按钮。
- 可能触发 GitHub 的二次安全验证（往你邮箱发 8 位验证码），把验证码填进输入框点 `Verify`。
- 完成后终端显示：
  ```
  ✓ Logged in to github.com as kekeliang1983-ship-it
  ```

**4. 验证登录**
```powershell
gh auth status
```
应显示 `✓ Logged in to github.com as kekeliang1983-ship-it`。

---

### 步骤二：本地 git 仓库准备（本项目已做，记录备查）

> 以下命令在项目目录 `NEW OBB/CozyCreatures` 内执行。

**1. 初始化仓库 + 改默认分支为 `main`**
```bash
git init
git branch -m master main      # 把默认分支从 master 改成 GitHub 标准的 main
```

**2. 配置提交身份**
```bash
git config user.name "kekeliang1983-ship-it"
git config user.email "kekeliang1983-ship-it@users.noreply.github.com"
```

**3. `.gitignore`（已含完整规则，关键点见踩坑节）**
项目根目录 `.gitignore` 已配置，核心规则：
```gitignore
# 构建产物（含历次规避 EPERM 用的 dist-relN / dist2 / dist_deploy / dist-single / dist-tmp / dist_xxx 等变体）
dist/
dist*/
node_modules/

# 环境变量敏感文件
.env
.env.local
.env.*.local

# 调试
.vite/
*.log
.DS_Store

# vite 构建临时文件
vite.config.ts.timestamp-*.mjs

# 代码分析
stats.html
```
> 为什么需要 `dist*/` 和 `vite.config.ts.timestamp-*.mjs`：详见「第五节踩坑」。

**4. 部署工作流文件** `.github/workflows/deploy.yml`（**完整内容如下，请勿改动结构**）
```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run build
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
```
**这套工作流做了什么**：你 `push` 到 `main` → 在 GitHub 云端 Ubuntu 上 `npm ci` 装依赖 → `npm run build` 构建出 `dist/` → 上传为 Pages 产物 → 部署到 GitHub Pages。**全程自动，你零手动操作。**

---

### 步骤三：提交源码

**1. 添加并提交（注意清理构建垃圾，见踩坑）**
```bash
git add -A
git commit -m "feat: CozyCreatures 治愈小生物 管理后台 + 竞速深度增强"
```
本项目最终提交了 3 个 commit：
```
12e550e chore: .gitignore 增补 vite 临时文件规则
65b2d30 chore: 清理 vite 构建临时文件并加入 .gitignore
f8c59a5 feat: CozyCreatures 治愈小生物 管理后台 + 竞速深度增强
```

> ⚠️ 重要：`git add -A` 会把 vite 构建遗留的 `vite.config.ts.timestamp-*.mjs` 临时文件也收进来，必须清理（见第五节）。

---

### 步骤四：创建根仓库并推送

**1. 建仓库 + 推送（一条命令搞定）**
```powershell
gh repo create kekeliang1983-ship-it.github.io --public --source . --push `
  --description "CozyCreatures · 治愈小生物 管理后台 + 竞速玩法"
```
- `--public`：公开仓库（Pages 免费）
- `--source .`：用当前目录初始化
- `--push`：建完直接推送当前分支
- 仓库名必须是 `<你的用户名>.github.io` 才是根仓库

**2. 启用 Pages（通过 API 设源为 GitHub Actions）**
```powershell
gh api -X POST /repos/kekeliang1983-ship-it/kekeliang1983-ship-it.github.io/pages `
  -f build_type=workflow
```
这一步**必须做**——新仓库默认没开 Pages，`deploy-pages` 那步会失败。设为 `build_type=workflow` 后，Pages 网址即 `https://kekeliang1983-ship-it.github.io/`。

---

### 步骤五：等待部署并验证

**1. 查看部署状态**
```powershell
gh run list --repo kekeliang1983-ship-it/kekeliang1983-ship-it.github.io --limit 3
gh run watch <run-id> --repo kekeliang1983-ship-it/kekeliang1983-ship-it.github.io
```
或直接在 GitHub 网页 → 仓库 → **Actions** 标签看进度。全流程约 1–2 分钟。

**2. 验证可访问（任意能联网的机器）**
```bash
curl -s -o /dev/null -w "首页        -> HTTP %{http_code}\n" https://kekeliang1983-ship-it.github.io/
curl -s -o /dev/null -w "race.json   -> HTTP %{http_code}\n" https://kekeliang1983-ship-it.github.io/content/race.json
curl -s -o /dev/null -w "sw.js       -> HTTP %{http_code}\n" https://kekeliang1983-ship-it.github.io/sw.js
```
期望全部返回 **HTTP 200**。本项目实测三者均 200，部署成功。

---

## 四、Service Worker 内容守卫（部署安全的核心）

线上「改 JSON 刷新即生效」依赖下面这道守卫。它位于 **`public/sw.js`**（注意：**不是** `src/sw.ts`，那是没参与部署的占位壳，详见踩坑）。

`public/sw.js` 第 54–59 行：
```javascript
  // 内容层保护：/content/*.json 永远走网络、禁用任何缓存，
  // 保证线上「改完 JSON → 玩家刷新即得」，不被 SW 预缓存/缓存优先拖成旧版。
  // （路径含 /content/ 即可兼容根部署与 GitHub Pages 子路径部署）
  if (p.includes('/content/')) {
    e.respondWith(fetch(req));
    return;
  }
```
- 用 `p.includes('/content/')` 而非开头匹配，兼容根部署和子路径部署。
- 这段必须放在「静态资源缓存优先」分支**之前**，优先生效。
- 本地 `npm run dev` 不受影响（SW 在 dev 下自动注销）。

---

## 五、踩坑与排错（重点，都是真金白银的教训）

### 坑 1：SW 改错了文件（最严重）
- **现象**：最初按直觉改了 `src/sw.ts` 加内容守卫，构建后核对产物 `dist/sw.js` 发现它和改的文件完全不同。
- **根因**：真正部署的 SW 是 **`public/sw.js`**（被 vite 原样拷进 dist）；`src/sw.ts` 只是占位壳，**根本不参与部署**。
- **后果**：那笔 `src/sw.ts` 的改动是「幽灵修复」，对线上零作用；而真实 SW 的「静态资源缓存优先」分支恰好会缓存 `/content/*.json`，正是要防的旧值风险。
- **正确做法**：守卫加在 `public/sw.js`；把 `src/sw.ts` 还原成占位壳原貌，避免以后误以为是活的。

### 坑 2：`git add -A` 误提交 vite 临时文件
- **现象**：首次 `git add -A` 把约 49 个 `vite.config.ts.timestamp-*.mjs` 临时文件收进了仓库。
- **解决**：`git rm --cached vite.config.ts.timestamp-*.mjs` 取消跟踪 + `.gitignore` 加 `vite.config.ts.timestamp-*.mjs` + 补提交清理。
- **磁盘残留**：这些文件在沙箱里是只读锁定，`rm -f`/`find -delete` 都删不掉（Permission Denied），但**已被 gitignore 忽略、不会进仓库、不影响推送**，放着无碍。

### 坑 3：构建 EPERM（沙箱拦截覆盖旧 dist）
- **现象**：`vite build` 覆盖旧 `dist` 里的 `.gz` 预压缩文件时，沙箱报 EPERM。
- **规避**：每次用全新输出目录，如 `npx vite build --mode production --outDir dist-rel7`。最终部署用 Actions（云端构建，无此问题），本地验证时才需此方法。

### 坑 4：本机 PortableGit bash 读不到 `gh` 凭据
- **现象**：对话里的 bash 执行 `gh auth status` 读不到登录态，但用户自己 CMD 里 `gh auth status` 显示已登录（`keyring`）。
- **原因**：对话环境用的 PortableGit bash，其凭据读取路径与用户终端不同。
- **结论**：`gh` 的登录凭据存在你机器上，你**自己终端里的 `gh`/`git` 会自动认**，无需传任何 token。建仓库/推送由你本机终端执行最稳。

### 坑 5：部署第一次可能失败（Pages 未开）
- **现象**：`gh repo create --push` 触发的第一次 Actions 部署，若当时 Pages 还没启用，`deploy-pages` 步骤会失败。
- **解决**：先 `gh api POST .../pages -f build_type=workflow` 启用 Pages，再等后续 push 触发的部署（或手动在 Actions 里 Re-run）。

### 坑 6：Actions 的 Node 版本弃用警告（无害）
- 工作流里 `actions/setup-node@v4` 指定 `node-version: 20`，GitHub 可能提示 Node 20 即将弃用（强制跑更高版本）。**部署仍成功**，仅警告。未来可升级 action 版本消除。

---

## 六、线上日常使用手册

### 6.1 如何更新「玩法数值」（最常用）
1. 在本机用编辑器/后台改 `public/content/*.json`（如 `race.json` 的天气、连击、结阵倍率）。
2. 提交并推送：
   ```bash
   git add -A
   git commit -m "content: 调整竞速天气与连击数值"
   git push
   ```
3. 等约 1 分钟，Actions 自动重部署。
4. **玩家刷新页面即得新版**（SW 不缓存 `/content/*`，不会被旧值拖住）。

### 6.2 如何更新「代码 / 新功能 / UI」
同上：`git push` 到 `main` 即自动重部署。JS/CSS 都带哈希命名，不会串版本，安全。

### 6.3 如何查看部署是否成功
- GitHub 网页 → 仓库 → **Actions** 标签，看最近一次 run 是否全绿。
- 或命令行：`gh run list --repo kekeliang1983-ship-it/kekeliang1983-ship-it.github.io --limit 3`

### 6.4 访问地址
- 游戏（公网）：https://kekeliang1983-ship-it.github.io/
- 开发+后台（仅本机）：进项目目录 `npm run dev` → http://localhost:5173/#/admin

### 6.5 内容加载原理（便于你以后接 CDN）
`src/stores/useContentStore.ts` 第 14–15 行：
```ts
/** 内容基址：默认同站 public/content；可经 VITE_CONTENT_BASE 指向 CDN 实现热更 */
const BASE = (import.meta.env.VITE_CONTENT_BASE as string | undefined) ?? '/content';
```
- 想让「改内容连部署都不用走」：把 `VITE_CONTENT_BASE` 设成一个你能随时改的地址（如独立 CDN/静态托管），App 一次部署后内容彻底外置，改远端 JSON 玩家刷新即得。
- 当前默认 `/content`（同站），已满足「push 即更新」。

---

## 七、后续可选项（本次未做，按需开启）

| 方向 | 内容 | 备注 |
|---|---|---|
| **B. 联机能力** | 结界符（已留 `onlineOnly` 桩）/ 访友 / 真人对战竞速（`beginRace` 已留真人匹配位）/ 漂流瓶跨用户投递 | 需后端服务器，GitHub Pages 不够，得上 WebSocket 服务 |
| **C. 运营侧** | 后台生产化（加鉴权，目前 admin 仅 dev）/ 自定义域名绑定 / CDN 迁移（`VITE_CONTENT_BASE`） | 根仓库已可绑自定义域名 |
| **D. 质量侧** | CI 断言 `content/*.json` schema，防后台误改崩 App / 真机发布渠道评估 | 可在 `deploy.yml` 的 build 前加 schema 校验步骤 |

---

## 八、命令速查表

```powershell
# —— 安装与登录（一次性）——
winget install --id GitHub.cli
gh auth login
gh auth status

# —— 创建仓库并推送（首次）——
gh repo create kekeliang1983-ship-it.github.io --public --source . --push `
  --description "CozyCreatures · 治愈小生物 管理后台 + 竞速玩法"
gh api -X POST /repos/kekeliang1983-ship-it/kekeliang1983-ship-it.github.io/pages -f build_type=workflow

# —— 日常更新（改完内容/代码后）——
git add -A
git commit -m "你的提交说明"
git push

# —— 查看部署 ——
gh run list --repo kekeliang1983-ship-it/kekeliang1983-ship-it.github.io --limit 3

# —— 本机开发 ——
npm install
npm run dev        # http://localhost:5173 （含后台 /#/admin）
npm run build      # 生产构建到 dist/
```

---

## 九、关键文件清单

| 文件 | 作用 |
|---|---|
| `.github/workflows/deploy.yml` | GitHub Actions 自动部署工作流（push→build→deploy） |
| `.gitignore` | 排除构建垃圾（dist*/、vite 临时文件、node_modules 等） |
| `public/sw.js` | **真实生效**的 Service Worker，含 `/content/*` 不缓存守卫 |
| `src/sw.ts` | 占位壳，**不参与部署**，勿在此改 SW 逻辑 |
| `src/stores/useContentStore.ts` | 内容加载，`BASE='/content'`（可经 `VITE_CONTENT_BASE` 指向 CDN） |
| `vite.config.ts` | `base: '/'`（根仓库成立），`npm run build` 脚本 |
| `public/content/*.json` | 所有外置玩法数值（天气/连击/结阵/连胜/称号…） |
| `package.json` | `build` 脚本：`vue-tsc --noEmit && vite build --mode production` |

---

> **维护提示**：本文档与项目同源（已建议在仓库内维护）。后续若改动部署架构（如换自定义域名、加 CDN、改普通仓库），请同步更新本文档对应章节。
